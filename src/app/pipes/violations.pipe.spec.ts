import { ViolationsPipe } from './violations.pipe';

describe('ViolationsPipe', () => {
  it('create an instance', () => {
    const pipe = new ViolationsPipe();
    expect(pipe).toBeTruthy();
  });
});
